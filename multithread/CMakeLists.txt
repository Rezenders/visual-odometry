cmake_minimum_required(VERSION 2.8)
project(opencv-mt)
find_package( Threads REQUIRED )
find_package( OpenCV REQUIRED )
add_executable( opencvmt main.cpp )
target_link_libraries( opencvmt ${OpenCV_LIBS} ${CMAKE_THREAD_LIBS_INIT} )
